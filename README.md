# global-dark-light-mode-extension
A smart Chrome extension built with JavaScript (Web Extensions API) for global dark/light mode toggling, per-site theme overrides, and Chrome storage preference synchronization.
